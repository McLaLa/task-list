<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mclala.tasklist">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.mclala.tasklist.MainActivity"
            android:theme="@style/WATheme"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mclala.tasklist.ListActivity"
            android:label="@string/item_title"
            android:theme="@style/WATheme"
            android:screenOrientation="portrait"
            android:parentActivityName="com.mclala.tasklist.MainActivity" >
        </activity>

        <activity android:name="com.mclala.tasklist.AboutActivity"
            android:screenOrientation="portrait"/>


    </application>

</manifest>
